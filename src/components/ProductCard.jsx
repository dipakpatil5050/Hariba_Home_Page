import React from "react";

function ProductCard() {
  return (
    <>
      <div className="w-32 card h-52 ml-40 bg-gray-500 rounded-xl p-44 border flex flex-row item-center m-10">
        {/* {img1.map((item) => (
          <div key={index}>
            <img src={item.img} alt="" />
          </div>
        ))}

        <img className="" src={props.img} alt={props.alt} /> */}
      </div>
    </>
  );
}

export default ProductCard;
